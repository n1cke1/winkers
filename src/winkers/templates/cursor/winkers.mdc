---
description: Architectural context workflow using Winkers dependency graph
globs:
  - "**/*.py"
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.go"
  - "**/*.java"
alwaysApply: false
---

# Winkers — Architectural Context

Before modifying code, use the Winkers MCP tools to understand the
dependency graph and avoid breaking callers.

## Workflow

1. **Understand structure**
   `map()` — constraints, conventions, zones, hotspots. Call first.

2. **Inspect the area**
   `map(zone="<zone>")` or `map(file="<path>")` — narrow to specific zone/file.

3. **Get constraints**
   `scope(file="<path>")` — for each file you'll modify.
   Returns locked functions, their callers, safe vs breaking changes.

4. **After writing code**
   `scope(function="<name>")` — verify callers are not broken.

## Other tools

- `functions_graph()` — full indexed function list with caller counts.
- `hotspots()` — functions with many callers; high-impact to change.
- `routes()` — HTTP endpoints: method, path, handler, callees.

## Key rules

- **locked** function = has callers depending on signature.
  Never change param types, order, or return type without updating all callers.
- **free** function = no callers. Modify freely.
