---
description: Architectural context workflow using Winkers dependency graph
globs:
  - "**/*.py"
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "**/*.go"
  - "**/*.java"
alwaysApply: false
---

# Winkers — Architectural Context

Before modifying code, use the Winkers MCP tools to understand the
dependency graph and avoid breaking callers.

## Workflow

1. **Understand structure**
   `orient(["map","conventions"])` — zones, hotspots, data flow, zone intents. Call first.

2. **Inspect the area**
   `scope(file="<path>")` — locked/free functions, callers, related rules for that file.

3. **Get rules**
   `orient(["rules_list"])` — available coding rule categories.
   `rule_read("<category>")` — detailed rules with wrong_approach.

4. **After writing code**
   `scope(function="<name>")` — verify callers are not broken.

## Other tools

- `orient(["functions_graph"])` — full indexed function list with caller counts.
- `orient(["hotspots"])` — functions with many callers; high-impact to change.
- `orient(["routes"])` — HTTP endpoints: method, path, handler, callees.
- `orient(["ui_map"])` — route-to-template links with UI elements.
- `convention_read("<zone>")` — zone intent details, data_flow, checklist.

## Key rules

- **locked** function = has callers depending on signature.
  Never change param types, order, or return type without updating all callers.
- **free** function = no callers. Modify freely.
